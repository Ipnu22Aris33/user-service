export enum TokenTypeEnum {
  ACCESS = 'ACCESS',
  REFRESH = 'REFRESH',
  VERIFICATION = 'VERIFICATION',
  RESET_PASSWORD = 'RESET_PASSWORD',
}
export const TokenTypeEnumType = Object.values(TokenTypeEnum);
